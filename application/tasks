1.	При добавлении нового ряда-метода, данные в колонках «Дата проведения ГИС»,                                             С клонированием(дат) пока проблемы.
    «Дата поступления ГИС в ИГиРГИ» и «Интервал записи (факт), м» должны по умолчанию копироваться из                       Сейчас просто копируется инпут без datepicker
    первой строки и меняться только если редактировать их вручную.

2.	Ввести кнопку удаления рядов                                                                                            done

3.	Все расчетные проценты округлить до целого                                                                              done

4.	Для Плотности данных Real-time внести 2 опции: «более 5 точек на метр»                                                  done
    (зеленым цветом) и «менее 5 точек на метр» (красным цветом)

5.	Добавить разделение приборов по методам при их добавлении (видимо, это уже добавлено, но нужно, чтобы и отображалось    done

6.	Зафиксировать ширины всех колонок (они меняются, если, например,                                                        Отмена
    изменить название метода с более длинного на более короткое или наоборот)

7.	В примечаниях иногда бывает глюк, при котором ширина строки не меняется                                                 Оставить на самый конец (textarea)
    при добавлении новых строк текста; при этом выравнивание происходит не по центру,
    а по верху ячейки, и верхние ряды уходят наверх за пределы ячейки, становясь невидимыми.




!!!!!!!!!!!!!!!!!!!!!
1. Неблокирующий доступ к БД
